<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand p-3" href="{{ path('team_index') }}">Tournoi des nations</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                    Équipes
                </a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('team_index') }}">Classement des équipes</a>
                    <a class="dropdown-item" href="{{ path('team_stats') }}">Statistiques des équipes</a>
                    <a class="dropdown-item" href="{{ path('team_create') }}">Créer une équipe</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                    Tournois
                </a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('tournament_create') }}">Créer un tournoi</a>
                    <a class="dropdown-item" href="{{ path('tournament_index') }}">Liste des tournois</a>
                </div>
            </li>
        </ul>
    </div>
</nav>